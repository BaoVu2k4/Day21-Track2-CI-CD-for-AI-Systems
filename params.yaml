# Sieu tham so cho mo hinh RandomForestClassifier
# Thay doi cac gia tri nay giua cac lan chay de thi nghiem (Buoc 1)
# Best params: n_estimators=800, max_depth=None -> accuracy=0.684 (phase1), 0.75+ (phase2)
n_estimators: 800
max_depth: null
min_samples_split: 2
